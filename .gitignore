# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build-time configuration; only the example is tracked
.env
.env.*
!.env.example

# Wrangler local dev state / build cache
.wrangler/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cached catalogue responses used by the dataset verification scripts
scripts/.cache/

# Working notes, not part of the build
docs/superpowers/

# D1 migrations, kept out of the repo by choice; the schema is recorded in the README instead
migrations/
